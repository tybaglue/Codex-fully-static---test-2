// Duplicate this file as config.js and update the values before deploying.

export const SUPABASE_URL = "https://your-project.supabase.co";
export const SUPABASE_ANON_KEY = "public-anon-key";

// Optional: set to true to require authentication before using the dashboard.
export const ENABLE_AUTH = false;

// When authentication is on, only these email domains can sign up.
export const ALLOWED_EMAIL_DOMAINS = ["kewgardenflowers.com"];
