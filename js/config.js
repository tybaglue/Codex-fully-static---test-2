// Copy your Supabase project keys here before deploying.
// These placeholder values will block calls until updated.

export const SUPABASE_URL = window.SUPABASE_URL || "https://your-project.supabase.co";
export const SUPABASE_ANON_KEY = window.SUPABASE_ANON_KEY || "public-anon-key";

export const ENABLE_AUTH = window.ENABLE_AUTH ?? false;
export const ALLOWED_EMAIL_DOMAINS = window.ALLOWED_EMAIL_DOMAINS || [
  "kewgardenflowers.com",
];
